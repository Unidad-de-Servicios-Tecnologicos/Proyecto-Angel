<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <corners android:radius="6dp"/>
            <solid android:color="@color/black_gray" /> <!-- Color de fondo por defecto -->
            <stroke
                android:width="1dp"
                android:color="@color/gray"/>
        </shape>
        <color android:color="@color/gray" /> <!-- Color de texto por defecto -->
    </item>
</selector>